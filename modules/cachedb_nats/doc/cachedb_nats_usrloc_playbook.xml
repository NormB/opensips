<!--
  Operator playbook: cachedb_nats as the storage backend for
  usrloc in cluster_mode=full-sharing-cachedb.

  This file is a DocBook XML fragment included via an entity
  reference from cachedb_nats_admin.xml as the closing section
  of the admin chapter.  It documents topology, bucket sizing,
  snapshot/restore, alert thresholds, scale tuning (PK fast
  path, optional index), and migration procedures.

  Do NOT add an XML prolog or DOCTYPE here.  The entity
  reference splices this file's content directly into the
  including chapter, and a duplicate prolog mid-document is a
  fatal parse error.
-->

<section id="usrloc_storage_playbook" xreflabel="usrloc storage playbook">
	<title>Operator playbook: NATS as backend store for usrloc</title>

	<para>
		This section is for operators deploying
		<varname>usrloc</varname> in
		<varname>cluster_mode = full-sharing-cachedb</varname> with
		<varname>cachedb_nats</varname> as the cachedb engine.  In
		this configuration JetStream KV is the authoritative store
		for AoR / contact records:
		<itemizedlist>
			<listitem><para>SIP REGISTER → <function>save("location")</function>
				→ usrloc → <function>cdbf.update</function> →
				<function>kvStore_UpdateString</function> /
				<function>kvStore_CreateString</function> on
				<literal>KV_&lt;bucket&gt;.json_&lt;encoded-aor&gt;</literal>.
			</para></listitem>
			<listitem><para>SIP lookup → usrloc →
				<function>cdb_load_urecord</function> →
				<function>nats_cache_query</function> using the
				per-process JSON-FTS index over JetStream KV.
			</para></listitem>
			<listitem><para>Process restart → usrloc state in shared
				memory is lost; cachedb_nats's
				<function>nats_json_index_build</function> rehydrates
				the index from the bucket.
			</para></listitem>
		</itemizedlist>
		Multiple OpenSIPS instances pointing at the same bucket
		converge on a consistent view of usrloc state without any
		clusterer-driven binary replication; the only
		synchronization between writers is JetStream's per-key CAS.
	</para>

	<section>
		<title>Topology by DC count</title>
		<variablelist>
			<varlistentry>
				<term>Single DC, single broker</term>
				<listitem><para>
					<varname>kv_replicas = 1</varname>,
					<varname>kv_history = 1</varname>,
					File storage.  Use this for development and
					small single-DC deployments.  Restart hydration
					is fast (one round-trip per key).
				</para></listitem>
			</varlistentry>
			<varlistentry>
				<term>Single DC, clustered NATS (3 servers)</term>
				<listitem><para>
					<varname>kv_replicas = 3</varname>.  Survives a
					single broker failure with no AoR loss.  Recommended
					production baseline.  Inter-broker replication is
					NATS-internal and adds ~1-2 ms to write latency.
				</para></listitem>
			</varlistentry>
			<varlistentry>
				<term>Multiple DCs, one global bucket</term>
				<listitem><para>
					Recommended only when the inter-DC RTT is
					&lt; 50 ms and concurrent same-AoR writes are
					rare.  Every <function>kvStore_UpdateString</function>
					CAS pays full inter-DC RTT.  The jittered
					backoff bounds the worst-case at ~50 ms across
					the default <varname>nats_cas_retries = 10</varname>
					budget; raise the budget if cross-DC
					contention drives <literal>cas_exhausted</literal>
					above zero.
				</para></listitem>
			</varlistentry>
			<varlistentry>
				<term>Multiple DCs, bucket per DC + leafnodes</term>
				<listitem><para>
					Lower per-write latency at the cost of cross-DC
					visibility being asynchronous.  Each DC reads
					its local bucket; cross-DC AoR resolution
					happens out of band (e.g.  GSLB or local-fail-
					through-to-remote logic).  Conflict resolution
					becomes operator-defined; the simplest policy is
					"the first DC to see a REGISTER owns the AoR."
				</para></listitem>
			</varlistentry>
		</variablelist>
	</section>

	<section>
		<title>Bucket sizing</title>
		<itemizedlist>
			<listitem><para><emphasis>Storage:</emphasis>
				<varname>File</varname> (default).  Memory storage
				loses AoRs on broker restart, defeating the purpose
				of using NATS as the backend store.
			</para></listitem>
			<listitem><para><emphasis>Replicas:</emphasis> match
				broker cluster size up to 3.  Replicas &gt; 3 add
				latency without meaningful availability gains for
				usrloc workloads.
			</para></listitem>
			<listitem><para><emphasis>History:</emphasis>
				<varname>kv_history = 1</varname> — required for
				on-time native expiry.  A bucket that keeps old
				revisions makes an expired key roll back to a
				previous version instead of disappearing, so the
				module disables server-side auto-expiry on it and
				falls back to the periodic cleanup scan (correct,
				but up to <varname>nats_reap_interval</varname>
				seconds late).  Use a history-keeping bucket only
				when routing logic genuinely needs
				<function>nats_kv_history</function>, and keep it
				OFF the registration bucket if possible.
			</para></listitem>
			<listitem><para><emphasis>TTL:</emphasis>
				<varname>kv_ttl = 0</varname> (no bucket-wide
				max age — it would delete permanent contacts; the
				module refuses to start otherwise).  Per-AoR
				expiry is native: each registration write carries
				its own server-side TTL, with the cleanup scan
				(<varname>nats_reap_interval</varname>) as the
				safety net.  To keep expired records readable for
				a while (forensics), see
				<varname>nats_expired_linger</varname>.
			</para></listitem>
			<listitem><para><emphasis>Max bytes / max age:</emphasis>
				NATS defaults are typically appropriate.  For
				bounded environments, size the stream for at least
				<literal>(active_aors × avg_doc_size × kv_history)</literal>
				bytes.  An average AoR JSON document with a single
				contact is roughly 400-600 bytes.
			</para></listitem>
		</itemizedlist>
	</section>

	<section>
		<title>Snapshot and restore</title>
		<para>
			JetStream KV buckets are JetStream streams named
			<literal>KV_&lt;bucket&gt;</literal> under the hood, so
			they snapshot and restore through the standard stream
			tooling:
			<programlisting format="linespecific">
nats stream snapshot KV_USRLOC ./usrloc-2026-05-08.tar.gz
nats stream restore  KV_USRLOC ./usrloc-2026-05-08.tar.gz
			</programlisting>
		</para>
		<para>
			Snapshot during a low-traffic window for a
			point-in-time copy.  After restore, every connected
			OpenSIPS instance must rebuild its in-memory index;
			either bounce the instances (cleanest), set
			<varname>index_resync_on_reconnect = 1</varname> and
			cycle the broker, or set
			<varname>index_resync_interval_secs</varname> to a
			value &le; the longest tolerable staleness window so
			the periodic timer pulls the new state.  The lazy
			self-heal in <function>nats_cache_query</function>
			only evicts stale entries it already holds; it does
			not add keys introduced by the restore, so an explicit
			rebuild (or the periodic timer) is required, not merely
			faster.
		</para>
	</section>

	<section id="usrloc_storage_scale_tuning"
		xreflabel="usrloc storage: scale tuning">
		<title>Scale tuning: PK fast path and the optional index</title>
		<para>
			usrloc&apos;s read and write path is exclusively
			<emphasis>primary-key</emphasis>: both
			<function>cdb_load_urecord</function> (read) and
			<function>cdb_flush_urecord</function> (write) build
			<varname>cdb_filter_t</varname> with
			<varname>is_pk = 1</varname>.  The in-memory JSON-FTS
			search index that <function>cachedb_nats</function>
			maintains is therefore <emphasis>optional weight</emphasis>
			for any deployment that uses the module purely as a
			usrloc backend.  Three operator knobs control the index
			behaviour:
		</para>
		<variablelist>
			<varlistentry>
				<term><varname>dedicated_watcher_proc</varname>
				(int, default 0)</term>
				<listitem><para>
					When set to <literal>1</literal> (and the
					index is on), the module declares a
					<varname>proc_export_t</varname> entry and the
					OpenSIPS core forks a dedicated child process
					that owns the KV watcher loop.  Rank&apos;s 1
					SIP worker no longer runs the watcher pthread,
					so the steady-state watcher event rate
					(~1 700 events/s at 100k AoRs, ~17 % of one
					core) stops competing with SIP request
					handling on rank 1.  Ignored when
					<varname>enable_search_index = 0</varname>
					(no index, no watcher).  Only worth flipping
					when the deployment keeps the index on at
					&ge; 100k AoRs and rank-1 latency matters.
				</para></listitem>
			</varlistentry>
			<varlistentry>
				<term><varname>enable_search_index</varname>
				(int, default 1)</term>
				<listitem><para>
					When set to <literal>0</literal>, the module
					skips <function>nats_json_index_init</function>
					entirely, never builds the index from KV at
					startup, and never starts the watcher thread.
					<function>nats_cache_query</function> handles
					every read through the new PK fast path
					(<function>kvStore_Get</function> on the
					encoded primary key); non-PK or
					multi-condition filters are rejected with an
					explicit error.
					<function>nats_cache_update</function> uses
					its long-existing PK branch for writes; non-PK
					updates are likewise rejected.  This is the
					recommended setting for pure usrloc-as-store
					deployments at every scale.
				</para></listitem>
			</varlistentry>
			<varlistentry>
				<term><varname>index_buckets</varname>
				(int, default 4096)</term>
				<listitem><para>
					Hash bucket count for the JSON-FTS index.
					Init code rounds the value up to the next
					power of two with a floor of 16
					(<varname>NATS_IDX_SHARDS</varname>) so the
					hash can use a bitmask AND.  Each doubling
					halves average chain length for ~32 KB more
					SHM.  Only relevant when
					<varname>enable_search_index = 1</varname>;
					ignored otherwise.  Recommended values:
					4096 at &le; 20k AoRs, 16384 at 100k, 65536
					at 1MM.
				</para></listitem>
			</varlistentry>
			<varlistentry>
				<term>PK fast path
					(<function>nats_cache_query</function>)</term>
				<listitem><para>
					Always on; no modparam.  When the filter is a
					single is_pk=1 condition with
					<varname>val.is_str = 1</varname> and
					<varname>op = CDB_OP_EQ</varname>, the function
					encodes the value via
					<function>_kv_encode_key</function>, builds the
					prefixed target_key, calls
					<function>kvStore_Get</function>, parses the
					returned JSON, and returns one row.  No mutex
					acquired, no chain walk.  Falls through to the
					index path for any non-PK or multi-condition
					filter.
				</para></listitem>
			</varlistentry>
		</variablelist>
		<para>
			Scaling guidance is documented in detail in
			<filename>doc/SCALING.md</filename>; the short version:
		</para>
		<itemizedlist>
			<listitem><para>
				<emphasis>Pure usrloc workloads at any scale:</emphasis>
				set <varname>enable_search_index = 0</varname>.
				No index, no watcher, no per-write index update.
			</para></listitem>
			<listitem><para>
				<emphasis>Mixed workloads (script-driven non-PK
				queries on the same bucket):</emphasis> leave the
				index on and tune <varname>index_buckets</varname>.
				At &ge; 100k AoRs also set
				<varname>dedicated_watcher_proc = 1</varname> to
				move the watcher out of the rank-1 SIP worker.
			</para></listitem>
			<listitem><para>
				<emphasis>1MM endpoints:</emphasis> the index off
				is essentially mandatory (~250 MB SHM saved per
				instance); the watcher would otherwise saturate
				one core at the steady-state event rate.  If
				the index is still kept on for mixed-workload
				reasons,
				<varname>dedicated_watcher_proc = 1</varname>
				is required.
			</para></listitem>
			<listitem><para>
				<emphasis>10MM endpoints:</emphasis> the index
				off is mandatory.  Sharding remains required for
				SIP-side reasons.
			</para></listitem>
		</itemizedlist>
	</section>

	<section>
		<title>SHM allocator selection</title>
		<para>
			OpenSIPS defaults the shared-memory allocator to
			<varname>Q_MALLOC</varname>, which is single-mutex
			and serialises every <function>shm_malloc</function>
			/ <function>shm_free</function> across all worker
			processes.  At populated index sizes (10k+ AoRs) and
			sustained REGISTER churn this becomes the dominant
			tail-latency contributor: the per-CAS path triggers
			3-5 SHM allocations to add a doc to the in-memory
			JSON-FTS index, and Q_MALLOC's lock pinches every
			worker on every alloc.
		</para>
		<para>
			Recommendation: launch <command>opensips</command>
			with <varname>-s HP_MALLOC</varname> to select
			<varname>HP_MALLOC</varname> ("optimized for shared
			memory multiprocessing").  Bench results from the
			session that introduced this playbook:
			<itemizedlist>
				<listitem><para>
					10k AoRs, single instance:
					p99 84 ms → 54 ms (−36%);
					p50 35 ms → 21 ms (−40%).
				</para></listitem>
				<listitem><para>
					20k AoRs, single instance:
					p99 185 ms → 142 ms (−23%);
					p50 41 ms → 25 ms (−39%).
				</para></listitem>
			</itemizedlist>
			Combined with the targeted-index-remove change
			(<function>nats_json_index_remove_fields</function>),
			the cumulative p99 improvement at 20k AoRs is −63%
			versus the start of the perf series.  Effective RPS
			climbs accordingly.
		</para>
		<para>
			Pkg memory (<varname>-k</varname>) is per-process
			and not contended, so the default
			<varname>Q_MALLOC</varname> there is fine.  Only
			the SHM allocator (<varname>-s</varname>) needs the
			switch.
		</para>
	</section>

	<section>
		<title>MI counters and alert thresholds</title>
		<para>
			<function>nats_cdb_stats</function> returns a snapshot
			of four atomic per-process counters tracking the
			cachedb path:
			<itemizedlist>
				<listitem><para>
					<varname>cas_retry</varname> — bumped per
					failed UpdateString / CreateString race.  Steady
					non-zero values are normal under contention; a
					sustained high rate suggests
					<varname>nats_cas_retries</varname> is too low
					or the CAS hot key is genuinely contended.
				</para></listitem>
				<listitem><para>
					<varname>cas_exhausted</varname> — bumped when
					a CAS gave up after exhausting the budget.
					<emphasis>Should always be 0</emphasis> in a
					healthy deployment; non-zero indicates lost
					writes.  Page on
					<literal>delta(cas_exhausted) &gt; 0</literal>
					over any reasonable interval.
				</para></listitem>
				<listitem><para>
					<varname>create_doc</varname> — bumped per
					first-insert path (<function>kvStore_CreateString</function>
					of a seed doc).  Roughly equal to the rate of
					genuinely new AoRs.  A spike usually maps to a
					burst of new registrations.
				</para></listitem>
				<listitem><para>
					<varname>index_miss_kv</varname> — bumped when
					<function>nats_cache_query</function> hit an
					index entry whose KV key was already deleted.
					Self-heal evicts the entry; a non-zero rate is
					expected in multi-instance deployments.  A
					<emphasis>large</emphasis> sustained rate
					suggests cross-instance churn higher than the
					self-heal pace; consider enabling
					<varname>index_resync_interval_secs</varname>.
				</para></listitem>
			</itemizedlist>
		</para>
	</section>

	<section>
		<title>Migration from full-sharing-cachedb-cluster</title>
		<para>
			The legacy mode <varname>full-sharing-cachedb-cluster</varname>
			additionally relies on clusterer to broadcast cachedb
			invalidation events.  With cachedb_nats, the KV watcher
			thread + stale-entry self-heal subsume that role.  To
			migrate:
			<orderedlist>
				<listitem><para>
					Take a snapshot of the current bucket (above).
				</para></listitem>
				<listitem><para>
					On each instance, change <varname>cluster_mode</varname>
					to <varname>full-sharing-cachedb</varname>;
					keep the existing <varname>location_cluster</varname>
					(clusterer is still required for shtag /
					pinging coordination).
				</para></listitem>
				<listitem><para>
					Roll instances one at a time.  Each rolled
					instance rebuilds its index from KV on
					<function>child_init</function> and is
					immediately consistent with peers still on the
					old mode.
				</para></listitem>
				<listitem><para>
					After all instances are migrated, the
					clusterer-driven cachedb broadcast capabilities
					become idle and can be removed at the next
					maintenance window.
				</para></listitem>
			</orderedlist>
		</para>
	</section>

	<section>
		<title>Optional knobs introduced in this work</title>
		<para>
			See <xref linkend="usrloc_storage_scale_tuning"/> for
			<varname>enable_search_index</varname> and
			<varname>index_buckets</varname>, the two scale-tuning
			knobs.  The remaining knobs are listed below.
		</para>
		<variablelist>
			<varlistentry>
				<term><varname>index_resync_on_reconnect</varname>
				(int, default 1)</term>
				<listitem><para>
					Whether the KV watcher rebuilds the in-memory
					index in full after each reconnect.  The watcher
					uses <varname>UpdatesOnly</varname>, so writes made
					by sibling instances during an outage are never
					delivered live; the query-path self-heal only evicts
					stale entries it already holds and cannot recover a
					missed insert, so the default is on for correctness.
					Set to 0 only for large indexes that cannot afford
					the O(N) rebuild per reconnect, paired with
					<varname>index_resync_interval_secs</varname>.
				</para></listitem>
			</varlistentry>
			<varlistentry>
				<term><varname>index_resync_interval_secs</varname>
				(int, default 0 = off)</term>
				<listitem><para>
					Periodic full rebuild on a timer.  Belt-and-
					braces upper bound on per-process index
					staleness.  Skipped silently while NATS is
					disconnected.
				</para></listitem>
			</varlistentry>
			<varlistentry>
				<term><varname>nats_cas_retries</varname>
				(int, default 10)</term>
				<listitem><para>
					CAS budget per
					<function>nats_cache_update</function> /
					<function>nats_cache_counter_op</function>
					call.  Combined with the jittered
					backoff (50&nbsp;us base, 5&nbsp;ms cap),
					the worst-case per-call latency is
					~50&nbsp;ms.  Raise only if
					<varname>cas_exhausted</varname> shows
					non-zero rates; lowering risks lost writes
					under contention.
				</para></listitem>
			</varlistentry>
		</variablelist>
	</section>

</section>
