<?xml version="1.0" encoding="ISO-8859-1" ?>
<!--
  SIPp UAC scenario: User-Agent Rate Limiter Test

  Sends max_rate+1 requests with the same User-Agent header.
  The rate_limit_by_ua handler should allow the first N requests
  and reject the overflow with 429 Too Many Requests.

  Usage:
    sipp -sf rate_limit_ua_uac.xml 127.0.0.1:5060 -m 11 -r 50 -l 5

  Parameters:
    -m 11  : Send 11 messages total (assumes max_rate=10 in test config)
    -r 50  : 50 messages per second (burst to trigger limit quickly)
    -l 5   : Max 5 simultaneous calls

  Expected: first 10 get 200 OK, 11th gets 429 Too Many Requests
-->
<scenario name="UA Rate Limit Test">

  <!-- Send INVITE with a fixed User-Agent to trigger rate limiting -->
  <send retrans="500">
    <![CDATA[
      INVITE sip:ratetest@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      From: "Rate Test" <sip:ratelimit@[local_ip]>;tag=[pid][call_number]
      To: <sip:ratetest@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:ratelimit@[local_ip]:[local_port]>
      Max-Forwards: 70
      User-Agent: SIPp-RateLimitTest/1.0
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=- 1 1 IN IP4 [local_ip]
      s=Rate Limit UA Test
      c=IN IP4 [local_ip]
      t=0 0
      m=audio 6000 RTP/AVP 0
      a=rtpmap:0 PCMU/8000
    ]]>
  </send>

  <!-- Expect any final response (200 for allowed, 429 for rate limited) -->
  <recv response="." optional="false" timeout="5000">
    <action>
      <ereg regexp="429" search_in="msg" check_it="true" assign_to="is_429" />
    </action>
  </recv>

  <!-- Send ACK -->
  <send>
    <![CDATA[
      ACK sip:ratetest@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/UDP [local_ip]:[local_port];branch=[branch]
      From: "Rate Test" <sip:ratelimit@[local_ip]>;tag=[pid][call_number]
      To: <sip:ratetest@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Content-Length: 0

    ]]>
  </send>

</scenario>
