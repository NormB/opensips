<!-- Module User's Guide -->

<chapter>

	<title>&adminguide;</title>

	<section id="overview" xreflabel="Overview">
	<title>Overview</title>
	<para>
		The <emphasis>rust_http_webhook</emphasis> module provides
		fire-and-forget &http; POST webhook delivery for &sip; event
		payloads. Script functions return immediately while delivery
		happens asynchronously in a background tokio task within each
		worker process.
	</para>
	<para>
		This module is useful for pushing &sip; event data to external
		systems (CDR collectors, fraud detection, real-time analytics)
		without blocking &sip; message processing. If the internal queue
		is full, payloads are silently dropped and a counter is
		incremented.
	</para>
	<para>
		Features include custom &http; headers for authentication or
		metadata, fanout delivery to multiple &url;s, and retry with
		exponential backoff for transient failures.
	</para>
	</section>

	<section>
	<title>How it works</title>
	<para>
		Each &osips; worker process maintains its own independent
		fire-and-forget &http; dispatcher backed by a tokio runtime.
		When <function>rust_webhook()</function> is called from the
		script, the payload string is enqueued into a bounded channel.
		A background task dequeues payloads and sends them as &http;
		POST requests to the configured &url;(s).
	</para>
	<para>
		If the queue is full (exceeds <varname>max_queue</varname>),
		the payload is dropped and the internal drop counter is
		incremented. The script function always returns 1 (success)
		unless the dispatcher is not initialized.
	</para>
	<para>
		When multiple &url;s are configured (comma-separated), the
		payload is sent to ALL &url;s (fanout). Each &url; is handled
		independently — a failure on one does not affect the others.
	</para>
	<para>
		When <varname>max_retries</varname> is greater than 0, failed
		&http; requests (non-2xx responses or network errors) are
		retried with exponential backoff. The delay doubles after each
		attempt: <varname>retry_delay_ms</varname> * 2^(attempt-1).
	</para>
	<para>
		Custom headers can be added to every POST request using the
		<varname>headers</varname> parameter. This is useful for
		authentication tokens, source identification, or any custom
		metadata.
	</para>
	<para>
		The &http; client respects the configured timeout and content
		type. For HTTPS endpoints, a custom CA certificate file can
		be specified via <varname>tls_ca_file</varname>.
	</para>
	</section>

	<section id="dependencies" xreflabel="Dependencies">
	<title>Dependencies</title>
	<section>
		<title>&osips; Modules</title>
		<para>
		The following modules must be loaded before this module:
		<itemizedlist>
		<listitem><para>
			<emphasis>None</emphasis>.
		</para></listitem>
		</itemizedlist>
		</para>
	</section>
	<section>
		<title>External Libraries or Applications</title>
		<para>
		The following libraries or applications must be installed
		before running &osips; with this module loaded:
		<itemizedlist>
		<listitem><para>
			<emphasis>None</emphasis>. All &http; functionality is
			compiled into the Rust shared library.
		</para></listitem>
		</itemizedlist>
		</para>
	</section>
	</section>

	<section id="exported_parameters" xreflabel="Exported Parameters">
	<title>Exported Parameters</title>

	<section id="param_url" xreflabel="url">
		<title><varname>url</varname> (string)</title>
		<para>
		The HTTP(S) endpoint &url;(s) where webhook payloads are
		POSTed. This parameter is required. The module will fail to
		load if this parameter is not set or is empty.
		</para>
		<para>
		Multiple &url;s can be specified as a comma-separated list
		for fanout delivery. Each payload is sent to ALL &url;s.
		</para>
		<para>
		<emphasis>
			Default value is <quote>None</quote> (must be set).
		</emphasis>
		</para>
		<example>
		<title>Set <varname>url</varname> parameter (single)</title>
		<programlisting format="linespecific">
...
modparam("rust_http_webhook", "url", "https://example.com/hook")
...
</programlisting>
		</example>
		<example>
		<title>Set <varname>url</varname> parameter (fanout)</title>
		<programlisting format="linespecific">
...
modparam("rust_http_webhook", "url", "http://analytics:9090/events,http://backup:9091/events")
...
</programlisting>
		</example>
	</section>

	<section id="param_max_queue" xreflabel="max_queue">
		<title><varname>max_queue</varname> (integer)</title>
		<para>
		Maximum number of payloads that can be queued per worker
		process before new payloads are dropped. Each worker maintains
		its own independent queue. Must be at least 1; values below 1
		are clamped to 1 at module load.
		</para>
		<para>
		<emphasis>
			Default value is <quote>1000</quote>.
		</emphasis>
		</para>
		<example>
		<title>Set <varname>max_queue</varname> parameter</title>
		<programlisting format="linespecific">
...
modparam("rust_http_webhook", "max_queue", 2000)
...
</programlisting>
		</example>
	</section>

	<section id="param_http_timeout" xreflabel="http_timeout">
		<title><varname>http_timeout</varname> (integer)</title>
		<para>
		&http; request timeout in seconds. Requests that exceed this
		timeout are abandoned. Must be at least 1; values below 1
		are clamped to 1 at module load.
		</para>
		<para>
		<emphasis>
			Default value is <quote>5</quote>.
		</emphasis>
		</para>
		<example>
		<title>Set <varname>http_timeout</varname> parameter</title>
		<programlisting format="linespecific">
...
modparam("rust_http_webhook", "http_timeout", 10)
...
</programlisting>
		</example>
	</section>

	<section id="param_content_type" xreflabel="content_type">
		<title><varname>content_type</varname> (string)</title>
		<para>
		The Content-Type header value sent with each POST request.
		</para>
		<para>
		<emphasis>
			Default value is <quote>application/json</quote>.
		</emphasis>
		</para>
		<example>
		<title>Set <varname>content_type</varname> parameter</title>
		<programlisting format="linespecific">
...
modparam("rust_http_webhook", "content_type", "application/x-ndjson")
...
</programlisting>
		</example>
	</section>

	<section id="param_tls_ca_file" xreflabel="tls_ca_file">
		<title><varname>tls_ca_file</varname> (string)</title>
		<para>
		Path to a custom CA certificate file (PEM format) for TLS
		verification of the webhook endpoint. If not set, the system
		default CA bundle is used.
		</para>
		<para>
		<emphasis>
			Default value is <quote>None</quote> (system defaults).
		</emphasis>
		</para>
		<example>
		<title>Set <varname>tls_ca_file</varname> parameter</title>
		<programlisting format="linespecific">
...
modparam("rust_http_webhook", "tls_ca_file", "/etc/ssl/certs/custom-ca.pem")
...
</programlisting>
		</example>
	</section>

	<section id="param_headers" xreflabel="headers">
		<title><varname>headers</varname> (string)</title>
		<para>
		Custom &http; headers to include in every POST request.
		Multiple headers are separated by the pipe character
		(<literal>|</literal>). Each header is specified as
		<literal>Name: Value</literal>. Whitespace around names and
		values is trimmed. Entries without a colon are silently
		ignored.
		</para>
		<para>
		This is useful for authentication tokens, API keys, or
		source identification headers.
		</para>
		<para>
		<emphasis>
			Default value is <quote>None</quote> (no custom headers).
		</emphasis>
		</para>
		<example>
		<title>Set <varname>headers</varname> parameter</title>
		<programlisting format="linespecific">
...
modparam("rust_http_webhook", "headers", "Authorization: Bearer token123|X-Source: opensips")
...
</programlisting>
		</example>
	</section>

	<section id="param_max_retries" xreflabel="max_retries">
		<title><varname>max_retries</varname> (integer)</title>
		<para>
		Maximum number of retry attempts on &http; failure (non-2xx
		response or network error). Set to 0 to disable retries.
		Retries use exponential backoff with the base delay set by
		<varname>retry_delay_ms</varname>.
		</para>
		<para>
		<emphasis>
			Default value is <quote>0</quote> (no retries).
		</emphasis>
		</para>
		<example>
		<title>Set <varname>max_retries</varname> parameter</title>
		<programlisting format="linespecific">
...
modparam("rust_http_webhook", "max_retries", 3)
...
</programlisting>
		</example>
	</section>

	<section id="param_retry_delay_ms" xreflabel="retry_delay_ms">
		<title><varname>retry_delay_ms</varname> (integer)</title>
		<para>
		Base retry delay in milliseconds. The actual delay uses
		exponential backoff: <literal>retry_delay_ms *
		2^(attempt-1)</literal>. For example, with
		<varname>retry_delay_ms</varname>=1000 and
		<varname>max_retries</varname>=3, the delays are 1000ms,
		2000ms, and 4000ms.
		</para>
		<para>
		<emphasis>
			Default value is <quote>1000</quote>.
		</emphasis>
		</para>
		<example>
		<title>Set <varname>retry_delay_ms</varname> parameter</title>
		<programlisting format="linespecific">
...
modparam("rust_http_webhook", "retry_delay_ms", 500)
...
</programlisting>
		</example>
	</section>
	</section>

	<section id="exported_functions" xreflabel="exported_functions">
	<title>Exported Functions</title>

	<section id="func_rust_webhook" xreflabel="rust_webhook()">
		<title>
		<function moreinfo="none">rust_webhook(payload)</function>
		</title>
		<para>
		Enqueue a payload string for asynchronous &http; POST delivery
		to the configured webhook &url;(s). The function returns
		immediately without waiting for the &http; request to complete.
		</para>
		<para>
		If multiple &url;s are configured, the payload is sent to all
		of them (fanout). If the per-worker queue is full, the payload
		is silently dropped and a warning is logged.
		</para>
		<para>Meaning of the parameters is as follows:</para>
		<itemizedlist>
		<listitem><para>
			<emphasis>payload</emphasis> (string)  - The payload
			body to POST. Typically a JSON string constructed using
			pseudo-variables. This parameter is mandatory.
		</para></listitem>
		</itemizedlist>
		<para>Return codes:</para>
		<itemizedlist>
		<listitem><para>
			<emphasis>1</emphasis>  - payload enqueued (or
			dropped due to full queue).
		</para></listitem>
		<listitem><para>
			<emphasis>-1 (denied)</emphasis>  - missing parameter.
		</para></listitem>
		<listitem><para>
			<emphasis>-2 (error)</emphasis>  - dispatcher not
			initialized.
		</para></listitem>
		</itemizedlist>
		<para>
		This function can be used from REQUEST_ROUTE, FAILURE_ROUTE,
		and ONREPLY_ROUTE.
		</para>
		<example>
		<title><function>rust_webhook</function> usage</title>
		<programlisting format="linespecific">
...
route {
    rust_webhook("{\"method\":\"$rm\",\"from\":\"$fu\",\"ruri\":\"$ru\"}");
}
...
</programlisting>
		</example>
	</section>

	<section id="func_rust_webhook_stats" xreflabel="rust_webhook_stats()">
		<title>
		<function moreinfo="none">rust_webhook_stats()</function>
		</title>
		<para>
		Retrieve module statistics as a JSON string. Sets
		<literal>$var(webhook_stats)</literal> with a JSON object
		containing all tracked counters for the current worker process.
		</para>
		<para>Return codes:</para>
		<itemizedlist>
		<listitem><para>
			<emphasis>1</emphasis>  - always succeeds.
		</para></listitem>
		</itemizedlist>
		<para>
		This function can be used from REQUEST_ROUTE, FAILURE_ROUTE,
		and ONREPLY_ROUTE.
		</para>
		<example>
		<title><function>rust_webhook_stats</function> usage</title>
		<programlisting format="linespecific">
...
route {
    rust_webhook_stats();
    xlog("L_INFO", "Webhook stats: $var(webhook_stats)\n");
}
...
</programlisting>
		</example>
	</section>
	</section>

	<section id="exported_pseudo_variables" xreflabel="Exported Pseudo-Variables">
	<title>Exported Pseudo-Variables</title>
	<section>
		<title>$var(webhook_stats)</title>
		<para>
		JSON string containing the module statistics for the current
		worker process. Set by
		<function>rust_webhook_stats()</function>.
		</para>
	</section>
	</section>

	<section id="exported_statistics" xreflabel="Exported Statistics">
	<title>Exported Statistics</title>
	<section>
		<title><varname>sent</varname></title>
		<para>
		Number of webhook payloads successfully enqueued for delivery.
		</para>
	</section>
	<section>
		<title><varname>dropped</varname></title>
		<para>
		Number of webhook payloads dropped due to a full queue.
		</para>
	</section>
	<section>
		<title><varname>failed</varname></title>
		<para>
		Number of webhook payloads that failed to deliver (&http;
		errors, timeouts) after all retry attempts.
		</para>
	</section>
	<section>
		<title><varname>retried</varname></title>
		<para>
		Number of individual retry attempts made across all payloads.
		</para>
	</section>
	<section>
		<title><varname>retry_exhausted</varname></title>
		<para>
		Number of payloads that exhausted all retry attempts without
		a successful delivery.
		</para>
	</section>
	</section>

</chapter>
