<!-- Module User's Guide -->

<chapter>

	<title>&adminguide;</title>

	<section id="overview" xreflabel="Overview">
	<title>Overview</title>
	<para>
		The <emphasis>rust_refer_handler</emphasis> module tracks &sip;
		REFER transactions and their associated NOTIFY messages to
		provide a complete state machine for call transfer tracking.
		The module maintains per-worker transfer state keyed by
		Call-ID.
	</para>
	<para>
		This module is useful for monitoring the progress of &sip;
		call transfers (blind and attended) and taking action based
		on transfer success or failure.
	</para>
	</section>

	<section>
	<title>How it works</title>
	<para>
		The module implements a REFER/NOTIFY state machine with four
		states:
		<itemizedlist>
		<listitem><para>
			<emphasis>pending</emphasis>  - REFER received, no
			NOTIFY yet.
		</para></listitem>
		<listitem><para>
			<emphasis>trying</emphasis>  - NOTIFY with 100
			Trying received.
		</para></listitem>
		<listitem><para>
			<emphasis>success</emphasis>  - NOTIFY with 2xx
			status received.
		</para></listitem>
		<listitem><para>
			<emphasis>failed</emphasis>  - NOTIFY with 3xx-6xx
			status received.
		</para></listitem>
		</itemizedlist>
	</para>
	<para>
		Terminal states (success, failed) are sticky: once a transfer
		reaches a terminal state, subsequent NOTIFY messages do not
		change the status. The notify_count is still incremented.
	</para>
	<para>
		The script is responsible for parsing the Refer-To header and
		sipfrag status codes and passing them as parameters to the
		module functions. This keeps the module simple and testable.
	</para>
	<para>
		Stale REFER state is automatically expired after
		<varname>expire_secs</varname>. When the number of tracked
		transfers exceeds <varname>max_pending</varname>, an automatic
		sweep removes expired entries.
	</para>
	</section>

	<section id="dependencies" xreflabel="Dependencies">
	<title>Dependencies</title>
	<section>
		<title>&osips; Modules</title>
		<para>
		The following modules must be loaded before this module:
		<itemizedlist>
		<listitem><para>
			<emphasis>None</emphasis>.
		</para></listitem>
		</itemizedlist>
		</para>
	</section>
	<section>
		<title>External Libraries or Applications</title>
		<para>
		The following libraries or applications must be installed
		before running &osips; with this module loaded:
		<itemizedlist>
		<listitem><para>
			<emphasis>None</emphasis>.
		</para></listitem>
		</itemizedlist>
		</para>
	</section>
	</section>

	<section id="exported_parameters" xreflabel="Exported Parameters">
	<title>Exported Parameters</title>

	<section id="param_max_pending" xreflabel="max_pending">
		<title><varname>max_pending</varname> (integer)</title>
		<para>
		Maximum number of REFER transactions tracked per worker
		process. When this limit is reached, a sweep is triggered to
		remove expired entries. Must be at least 1; values below 1
		are clamped to 1 at module load.
		</para>
		<para>
		<emphasis>
			Default value is <quote>1000</quote>.
		</emphasis>
		</para>
		<example>
		<title>Set <varname>max_pending</varname> parameter</title>
		<programlisting format="linespecific">
...
modparam("rust_refer_handler", "max_pending", 2000)
...
</programlisting>
		</example>
	</section>

	<section id="param_expire_secs" xreflabel="expire_secs">
		<title><varname>expire_secs</varname> (integer)</title>
		<para>
		Time in seconds after which a tracked REFER transaction is
		considered stale and eligible for automatic removal. Must be
		at least 1; values below 1 are clamped to 1 at module load.
		</para>
		<para>
		<emphasis>
			Default value is <quote>300</quote> (5 minutes).
		</emphasis>
		</para>
		<example>
		<title>Set <varname>expire_secs</varname> parameter</title>
		<programlisting format="linespecific">
...
modparam("rust_refer_handler", "expire_secs", 600)
...
</programlisting>
		</example>
	</section>
	</section>

	<section id="exported_functions" xreflabel="exported_functions">
	<title>Exported Functions</title>

	<section id="func_handle_refer" xreflabel="handle_refer()">
		<title>
		<function moreinfo="none">handle_refer(refer_to)</function>
		</title>
		<para>
		Record a new REFER transaction. The Call-ID is automatically
		extracted from the &sip; message. The transfer state is
		initialized to <quote>pending</quote>.
		</para>
		<para>
		If a REFER for the same Call-ID already exists, it is
		overwritten with fresh state.
		</para>
		<para>Meaning of the parameters is as follows:</para>
		<itemizedlist>
		<listitem><para>
			<emphasis>refer_to</emphasis> (string)  - The value
			of the Refer-To header. Typically
			<literal>$hdr(Refer-To)</literal>. This parameter is
			mandatory.
		</para></listitem>
		</itemizedlist>
		<para>Return codes:</para>
		<itemizedlist>
		<listitem><para>
			<emphasis>1</emphasis>  - REFER recorded successfully.
		</para></listitem>
		<listitem><para>
			<emphasis>-2 (error)</emphasis>  - missing parameter,
			no Call-ID in message, or uninitialized worker.
		</para></listitem>
		</itemizedlist>
		<para>
		This function can be used from REQUEST_ROUTE, FAILURE_ROUTE,
		and ONREPLY_ROUTE.
		</para>
		<example>
		<title><function>handle_refer</function> usage</title>
		<programlisting format="linespecific">
...
route {
    if (is_method("REFER")) {
        if (handle_refer("$hdr(Refer-To)")) {
            sl_send_reply(202, "Accepted");
        } else {
            sl_send_reply(500, "Internal Error");
        }
    }
}
...
</programlisting>
		</example>
	</section>

	<section id="func_handle_notify" xreflabel="handle_notify()">
		<title>
		<function moreinfo="none">handle_notify(call_id, status_code)</function>
		</title>
		<para>
		Process a NOTIFY message for a tracked REFER transaction.
		Updates the state machine based on the sipfrag status code:
		<itemizedlist>
		<listitem><para>
			<emphasis>100</emphasis>  -&gt; trying
		</para></listitem>
		<listitem><para>
			<emphasis>200-299</emphasis>  -&gt; success (terminal)
		</para></listitem>
		<listitem><para>
			<emphasis>300-699</emphasis>  -&gt; failed (terminal)
		</para></listitem>
		</itemizedlist>
		</para>
		<para>Meaning of the parameters is as follows:</para>
		<itemizedlist>
		<listitem><para>
			<emphasis>call_id</emphasis> (string)  - The Call-ID
			of the dialog the NOTIFY belongs to. Typically
			<literal>$ci</literal>. This parameter is mandatory.
		</para></listitem>
		<listitem><para>
			<emphasis>status_code</emphasis> (string)  - The
			&sip; status code from the message/sipfrag body. Parsed
			as an integer internally. This parameter is mandatory.
		</para></listitem>
		</itemizedlist>
		<para>Return codes:</para>
		<itemizedlist>
		<listitem><para>
			<emphasis>1</emphasis>  - NOTIFY processed (matching
			REFER found).
		</para></listitem>
		<listitem><para>
			<emphasis>-1 (denied)</emphasis>  - no matching REFER
			found for this Call-ID.
		</para></listitem>
		<listitem><para>
			<emphasis>-2 (error)</emphasis>  - missing parameter
			or uninitialized worker.
		</para></listitem>
		</itemizedlist>
		<para>
		This function can be used from REQUEST_ROUTE, FAILURE_ROUTE,
		and ONREPLY_ROUTE.
		</para>
		<example>
		<title><function>handle_notify</function> usage</title>
		<programlisting format="linespecific">
...
route {
    if (is_method("NOTIFY")) {
        # Parse sipfrag body for status code
        if (handle_notify("$ci", "$var(sipfrag_code)")) {
            sl_send_reply(200, "OK");
        } else {
            sl_send_reply(481, "Subscription does not exist");
        }
    }
}
...
</programlisting>
		</example>
	</section>

	<section id="func_refer_status" xreflabel="refer_status()">
		<title>
		<function moreinfo="none">refer_status(call_id)</function>
		</title>
		<para>
		Query the current status of a tracked REFER transaction. Sets
		<literal>$var(refer_status)</literal> to one of:
		<quote>pending</quote>, <quote>trying</quote>,
		<quote>success</quote>, <quote>failed</quote>, or
		<quote>unknown</quote> (if no matching REFER is found).
		</para>
		<para>Meaning of the parameters is as follows:</para>
		<itemizedlist>
		<listitem><para>
			<emphasis>call_id</emphasis> (string)  - The Call-ID
			to query. Typically <literal>$ci</literal>. This parameter
			is mandatory.
		</para></listitem>
		</itemizedlist>
		<para>Return codes:</para>
		<itemizedlist>
		<listitem><para>
			<emphasis>1</emphasis>  - status found;
			<literal>$var(refer_status)</literal> set to the status
			string.
		</para></listitem>
		<listitem><para>
			<emphasis>-1 (denied)</emphasis>  - no matching REFER
			found; <literal>$var(refer_status)</literal> set to
			<quote>unknown</quote>.
		</para></listitem>
		<listitem><para>
			<emphasis>-2 (error)</emphasis>  - missing parameter
			or uninitialized worker.
		</para></listitem>
		</itemizedlist>
		<para>
		This function can be used from REQUEST_ROUTE, FAILURE_ROUTE,
		and ONREPLY_ROUTE.
		</para>
		<example>
		<title><function>refer_status</function> usage</title>
		<programlisting format="linespecific">
...
route[check_transfer] {
    if (refer_status("$ci")) {
        xlog("L_INFO", "transfer $ci status: $var(refer_status)\n");
        if ($var(refer_status) == "success") {
            # Transfer completed successfully
        }
        if ($var(refer_status) == "failed") {
            # Transfer failed - take recovery action
        }
    }
}
...
</programlisting>
		</example>
	</section>

	<section id="func_refer_stats" xreflabel="refer_stats()">
		<title>
		<function moreinfo="none">refer_stats()</function>
		</title>
		<para>
		Retrieve module statistics as a JSON string. Sets
		<literal>$var(refer_stats)</literal> with a JSON object
		containing all tracked counters for the current worker process.
		</para>
		<para>Return codes:</para>
		<itemizedlist>
		<listitem><para>
			<emphasis>1</emphasis>  - always succeeds.
		</para></listitem>
		</itemizedlist>
		<para>
		This function can be used from REQUEST_ROUTE, FAILURE_ROUTE,
		and ONREPLY_ROUTE.
		</para>
		<example>
		<title><function>refer_stats</function> usage</title>
		<programlisting format="linespecific">
...
route {
    refer_stats();
    xlog("L_INFO", "Refer stats: $var(refer_stats)\n");
}
...
</programlisting>
		</example>
	</section>
	</section>

	<section id="exported_pseudo_variables" xreflabel="Exported Pseudo-Variables">
	<title>Exported Pseudo-Variables</title>
	<section>
		<title>$var(refer_status)</title>
		<para>
		The current status of a tracked REFER transaction. Set by
		<function>refer_status()</function>. Possible values:
		<itemizedlist>
		<listitem><para>
			<quote>pending</quote>  - REFER received, awaiting
			NOTIFY.
		</para></listitem>
		<listitem><para>
			<quote>trying</quote>  - 100 Trying received.
		</para></listitem>
		<listitem><para>
			<quote>success</quote>  - 2xx received (transfer
			succeeded).
		</para></listitem>
		<listitem><para>
			<quote>failed</quote>  - 3xx-6xx received (transfer
			failed).
		</para></listitem>
		<listitem><para>
			<quote>unknown</quote>  - no matching REFER found.
		</para></listitem>
		</itemizedlist>
		</para>
	</section>
	<section>
		<title>$var(refer_stats)</title>
		<para>
		JSON string containing the module statistics for the current
		worker process. Set by
		<function>refer_stats()</function>.
		</para>
	</section>
	</section>

	<section id="exported_statistics" xreflabel="Exported Statistics">
	<title>Exported Statistics</title>
	<section>
		<title><varname>active_transfers</varname></title>
		<para>
		Number of currently tracked REFER transactions (not expired).
		</para>
	</section>
	<section>
		<title><varname>completed</varname></title>
		<para>
		Number of REFER transactions that reached
		<quote>success</quote> state.
		</para>
	</section>
	<section>
		<title><varname>failed</varname></title>
		<para>
		Number of REFER transactions that reached
		<quote>failed</quote> state.
		</para>
	</section>
	<section>
		<title><varname>expired</varname></title>
		<para>
		Number of REFER transactions that were swept due to expiry.
		</para>
	</section>
	<section>
		<title><varname>unknown_notify</varname></title>
		<para>
		Number of NOTIFY messages received with no matching REFER.
		</para>
	</section>
	</section>

</chapter>
